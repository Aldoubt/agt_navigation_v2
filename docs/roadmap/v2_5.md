# AGT Navigation V2.5 Roadmap

The current delivery line is:

```text
P0   First BT Mission                         PASS software integration; vehicle validation separate
V25-08 Architecture & Semantics Baseline
V25-09 Robust Route Navigation Core
V25-10 Sparse Global Correction / Anchor Recovery
V25-11 Local Environment Mapping
V25-12 Optional ESDF / Advanced Local Planning
```

GNSS / Wheel / GTSAM remain on the P1 state-estimation track. STD / Scan Context and
seasonal maps remain on the P2 research track.

## DONE

Existing stable foundations include the FAST-LIVO2 mapping adapter, registered
cloud and PCD persistence, ICP/NDT relocalization, Nav2 waypoint Action,
safety/chassis boundaries, semantic/keepout offline products, structured
system health/readiness, and the project `agt_mission_manager` business Action/FSM
boundary. “DONE” means implemented or supported by stated offline or bag evidence;
it is not a blanket vehicle acceptance claim.

## P0 — First BT Mission

- URDF collision-based self-filter geometry while preserving the existing
  CustomMsg input/output boundary. V25-02 implementation exists; raw/profile/URDF
  bag comparison and vehicle false-removal/missed-removal validation remain before DONE.
- Sensor health evidence. V25-03 implementation exists (`agt_sensor_monitor`) and the
  shared `/diagnostics` integration now caches each sensor-monitor status independently
  with freshness expiry; measured bag/vehicle threshold tuning remains pending.
- Semantic waypoint mode / schema 1.1. V25-04 core now defines typed
  `SemanticWaypointArray`, separates document/coverage/waypoint validation, and adds
  transactional `agt_semantic_map_server` waypoint mode. Qt authoring integration and
  local build/runtime validation remain before this item can be marked DONE.
- V25-05 capability layer IMPLEMENTED: `agt_bt_executor` provides BT.CPP 4.9
  factory registration, allowlisted smoke-tree loading, optional Groot2
  monitoring, structured readiness condition, and project Relocalize and
  ExecuteWaypointTask Action nodes. It is not in default bringup.
- V25-06 First BT Mission IMPLEMENTED: the controlled backend composes the
  allowlisted tree with project Actions, structured readiness/blocker evidence,
  safe execution identity, task hash validation, and bounded cancellation.
  Unit/contract validation and the V25-07 fake full-chain integration are
  complete; vehicle validation remains pending. The
  default `sequential` backend is unchanged.

## V25-08 — Architecture & Semantics Baseline

Freeze Navigation Capability, MAP/ROUTE/LOCAL, the three map products, state-estimation
ownership, Route/Runtime Path semantics, and future mode-aware readiness names. This
stage does not implement ROUTE or LOCAL runtime and does not add ROS interfaces.

## V25-09 — Robust Route Navigation Core

Route resolution, route segments, odometry-frame runtime paths and controller/path
follower integration remain future work.

## V25-10 — Sparse Global Correction / Anchor Recovery

Anchor-based correction and the single localization authority remain future work.

## V25-11 — Local Environment Mapping

Rolling local occupancy, raycast/log-odds and timeout behavior remain future work.

## V25-12 — Optional ESDF / Advanced Local Planning

ESDF is an optional derived representation after local occupancy; it is not a P1
default prerequisite.

## P1 / P2 tracks

GNSS, wheel factors and GTSAM/iSAM2 remain P1 state-estimation work. STD/Scan Context,
loop closure, long-term multi-layer maps and seasonal/structural agricultural
navigation remain P2 research.

## OPTIONAL

Operator and visualization enhancements that do not become business-state or
motion owners.

## Scope boundary

Semantic coverage, dynamic semantic perception, realtime traversability, and
long-term map algorithms remain separately gated capabilities. This roadmap does
not claim them as executable or vehicle-validated. P0 implementation status must
still distinguish source code present from bag/vehicle acceptance evidence.

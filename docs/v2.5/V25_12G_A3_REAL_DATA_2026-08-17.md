# V25-12G-A3 Real-Data Acceptance — 2026-08-17

## Scope

This document freezes the real-data acceptance evidence for V25-12G-A3 after the Site-Boundary reverse-admission amendment.

The acceptance scope remains:

```text
A3_LOCAL_MOTION_EVIDENCE_DIAGNOSTIC_NOT_ROUTE_READY
```

This is local motion evidence only. It does not establish start reachability, route readiness, global feasibility, or optimality.

## Verified code state

Branch:

```text
feat/v25-12g-maximum-feasible-coverage
```

Acceptance HEAD used by the operator:

```text
76939424  test(v25-12g): fix boundary admission error assertion
```

Relevant amendment commits immediately below that HEAD:

```text
203b3c3  fix(v25-12g): defer reverse footprint probe until needed
688924b  feat(v25-12g): report amended boundary admission funnel
433caf4  feat(v25-12g): route amended boundary evidence through A3
117cd53  feat(v25-12g): admit endpoint-safe boundary fallback
39ee971  feat(v25-12g): load amended boundary audit evidence
f7bd077  feat(v25-12g): split connector boundary evidence
2de9845  test(v25-12g): freeze A3 boundary admission orchestration
```

## Automated verification

The operator ran the focused amendment/A3 regression, package build, and package tests on ROS 2 Humble.

Observed package result:

```text
Summary: 249 tests, 0 errors, 0 failures, 0 skipped
```

No claim is made beyond this observed operator result.

## Real-data inputs

Runtime directory:

```text
/home/yangxuan/agt_navigation_v2/runtime/maps/agt_workbench_run
```

Canonical platform profile:

```text
profiles/platforms/mk_mini.yaml
```

The acceptance preflight verified the following inputs existed before derivation:

```text
vehicle_feasible_service_graph.yaml
vehicle_feasible_segments.yaml
turn_zones.yaml
navigation_map.yaml
navigation_map.pgm
site_boundary.yaml
derivation.yaml
aisle_graph.yaml
```

## Service-motion evidence

Observed service funnel:

```text
a2_service_state_count: 32
service_validation_count: 32
executable_service_action_count: 32
rejected_service_action_count: 0
unresolved_service_action_count: 0
ordinary_service_action_count: 28
dead_end_service_action_count: 4
executable_dead_end_service_action_count: 4
locally_validated_segment_count: 14
locally_validated_unique_coverage_length_m: 97.3566739352054
distinct_locally_validated_aisle_count: 5
```

Interpretation: all 32 A2 service states are locally executable under the current A3 service-motion validation, including all four dead-end exact-retrace actions. This remains local service-motion evidence only.

## Transition-motion evidence

Observed transition funnel:

```text
a2_connector_candidate_count: 40
transition_validation_count: 40
executable_transition_count: 0
forward_executable_transition_count: 0
reverse_executable_transition_count: 0
rejected_transition_count: 40
unresolved_transition_count: 0
```

All 40 A2 transition candidates remain non-executable under the current A3 connector backend chain.

## Site-Boundary amendment result

Observed amended boundary funnel:

```text
endpoint_boundary_conflict_count: 0
forward_path_boundary_conflict_count: 33
forward_path_boundary_reverse_admitted_count: 33
forward_path_boundary_reverse_executable_count: 0
forward_path_boundary_reverse_bounded_no_solution_count: 33
endpoint_boundary_conflict_reverse_admission_count: 0
```

This resolves the semantic question that motivated the amendment:

1. None of the 33 former broad Site-Boundary rejections were caused by an illegal connector endpoint footprint.
2. All 33 were endpoint-safe connectors for which the locally relevant forward Dubins family touched or crossed the Site Boundary along the path.
3. All 33 were therefore correctly admitted to the existing reverse-aware R6B fallback after the amendment.
4. None of those 33 produced a bounded reverse-aware solution under the current R6B backend/search envelope.

Therefore the former immediate hard rejection was too strong semantically, but correcting that classification did not recover an executable transition on this real dataset.

## Forward-evidence distribution

```text
HIGH_U | LOCAL_FORWARD_OCCUPANCY_BLOCKED           | 7
HIGH_U | LOCAL_FORWARD_PATH_SITE_BOUNDARY_CONFLICT | 29
LOW_U  | LOCAL_FORWARD_PATH_SITE_BOUNDARY_CONFLICT | 4
```

All final transition records ended as:

```text
REJECTED | NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION | 40
```

## R6B bounded-search evidence

All 40 connector candidates reached R6B after the amendment.

Observed expansion range:

```text
min expansions: 3
max expansions: 16621
```

Observed histogram:

```text
{3: 2, 7: 1, 9: 1, 62: 5, 89: 4, 90: 1, 162: 6, 273: 3, 274: 2,
 968: 2, 1806: 2, 1863: 1, 5774: 2, 8001: 2, 8017: 1, 11379: 2,
 16273: 1, 16621: 2}
```

The frozen R6B `max_expansions=30000` limit was not reached by any real-data connector. The observed no-solution results therefore must not be described as budget-limit saturation. They are bounded-search no-solution evidence under the current primitive set, discretization, collision/boundary constraints, goal connection logic, and search envelope.

They are not proofs of global kinematic infeasibility.

## Safety and contract invariants

Observed invariants:

```text
all_a2_service_states_preserved: True
all_a2_connector_candidates_preserved: True
dead_end_non_retrace_count: 0
endpoint_boundary_conflict_reverse_admission_count: 0
site_boundary_reverse_bypass_count: 0
forbidden_semantic_key_count: 0
```

Site Boundary remained hard throughout R6B. No endpoint-hard-conflict connector was admitted to reverse fallback.

## Runtime cost

Observed real-data derivation resources:

```text
User time: 1147.92 s
System time: 2.26 s
CPU: 100%
Wall time: 19:07.53
Maximum RSS: 73204 kB
Signals delivered: 0
Exit status: 0
```

The amended run is significantly more expensive because all 33 endpoint-safe forward-path boundary conflicts now receive the bounded R6B search they previously skipped.

## Asset integrity

The generated runtime sibling:

```text
vehicle_feasible_motion_graph.yaml
```

was strict-loaded successfully and reproduced byte-for-byte through the deterministic writer:

```text
schema: agt_vehicle_feasible_motion_graph/v1
status: MOTION_EVIDENCE_ONLY
services: 32
transitions: 40
A3 strict-load: PASS
A3 byte-stable rewrite: PASS
```

Hashes of all protected upstream inputs remained unchanged after derivation.

The runtime motion-graph asset is real-data evidence and is not a source-controlled canonical map asset.

## Local workspace state

After acceptance the operator still had only the previously unrelated local state:

```text
 M tools/rosbag_sensor_trimmer
?? tools/map_tools/render_pcd_top_views.py
```

These files were not touched by V25-12G-A3.

## Acceptance conclusion

Classification:

```text
A3 SERVICE MOTION: REAL-DATA SUPPORTED
A3 TRANSITION MOTION: ZERO EXECUTABLE EDGES UNDER CURRENT BACKEND
A3 SITE-BOUNDARY ADMISSION AMENDMENT: SEMANTICALLY SUPPORTED
VALIDATION SCOPE: DIAGNOSTIC ONLY / NOT ROUTE-READY
```

The next technical blocker is no longer Site-Boundary admission policy. It is the absence of any executable headland transition from the current bounded reverse-primitive connector backend on this dataset.

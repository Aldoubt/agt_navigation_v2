# V25-12G A3.7 Chain-Frontier RED Handoff

Date: 2026-08-18

Status: **A3.6 FROZEN / A4 BLOCKED / A3.7 TESTS-ONLY RED LANDED**

This handoff defines the strict target-machine RED gate for the bounded diagnostic-only stage:

```text
V25-12G A3.7 — A4 Chain-Frontier Failure Diagnosis
```

A3.7 does not modify planner behavior and does not re-run the A3.6 real-data derivation.

## 1. Repository / Branch

```text
Repository: Aldoubt/agt_navigation_v2
Branch: feat/v25-12g-maximum-feasible-coverage
Workspace: ~/agt_navigation_v2
ROS: ROS2 Humble
```

Use the original checkout only. Do not create another worktree.

Protected unrelated local paths remain:

```text
M  tools/rosbag_sensor_trimmer
?? src/agt_route_benchmark/
?? tools/map_tools/render_pcd_top_views.py
```

Never modify, reset, delete, rename, restore or stage them.

Do not use:

```text
git add .
git add -A
git clean
git reset --hard
git restore .
git checkout -- .
```

## 2. Frozen A3.6 Evidence

Formal evidence:

```text
docs/v2.5/V25_12G_A36_INTERMEDIATE_CURVATURE_2026-08-18.md
```

A3.6 result:

```text
baseline: 0 EXECUTABLE / 40 REJECTED
amended:  2 EXECUTABLE / 38 REJECTED
A36_RC=0
service_behavior_equal=True
connector_universe_preserved=True
protected_input_hashes_unchanged=True
forbidden_semantic_key_count=0
unexpectedly_regressed_connector_ids=[]
a4_entry_gate_passed=False
result=PARTIAL
real-data derivation executed exactly once
```

A4 remains blocked.

The saved target-machine A3.6 report is:

```text
/tmp/v25_12g_a36_intermediate_curvature.json
```

Do not re-run `tools/v25_12g_a36_intermediate_curvature_acceptance.py`.

## 3. A3.7 Question

A3.7 asks only:

> Which currently REJECTED connector(s), if exactly one of them were hypothetically recovered, would join one of the existing A3.6 EXECUTABLE transitions to satisfy the frozen A4 minimum two-transition chain predicate?

The frozen A4 predicate remains:

```text
2 continuous EXECUTABLE transitions
3 distinct segment_ids
>= 2 distinct aisle_ids
```

A3.7 does not make a connector executable. It identifies the engineering frontier to diagnose next.

## 4. Evidence Sources and Provenance Boundary

A3.7 consumes exactly two evidence sources:

```text
1. frozen A3.5/A3 topology and detailed historical diagnostics:
   runtime/maps/agt_workbench_run/vehicle_feasible_motion_graph.yaml

2. saved A3.6 current outcomes:
   /tmp/v25_12g_a36_intermediate_curvature.json
```

Important limitation:

The A3.6 JSON stores per connector only:

```text
status
backend_status
search_expansions
queue_exhausted
expansion_budget_reached
failure_class
```

It does NOT store the full A3.6 primitive-edge / goal-shot diagnostic counters.

Therefore future A3.7 output must keep two namespaces separate:

```text
a36_evidence
    current A3.6 outcome / termination evidence only

a35_reference
    pre-amendment historical detailed diagnostics from the frozen baseline graph
```

A3.5 detailed counters must never be serialized or described as A3.6 counters.

## 5. No Re-Derivation Contract

The future A3.7 harness path is:

```text
tools/v25_12g_a37_chain_frontier_diagnosis.py
```

It must NOT contain or call:

```text
derive_vehicle_feasible_motion_graph
derive_reverse_primitive_connector_plan
```

It must not require:

```text
vehicle profile
Navigation Grid
Site Boundary
Turn Zones
R6B configuration
```

It only loads the frozen baseline motion graph plus saved A3.6 JSON and prints a diagnostic JSON report to stdout.

No output/write flag is allowed.

## 6. Future A3.7 Public Contract

Frozen constants:

```text
REPORT_SCHEMA = agt_v25_12g_a37_a4_chain_frontier_diagnosis/v1
VALIDATION_SCOPE = A37_A4_CHAIN_FRONTIER_DIAGNOSTIC_ONLY_NOT_PLANNER_AMENDMENT_NOT_ROUTE_READY
A36_REPORT_SCHEMA = agt_v25_12g_a36_intermediate_curvature_acceptance/v1
EXPECTED_CONNECTOR_COUNT = 40
```

Required helpers:

```text
build_parser()
build_transition_view(baseline_payload, a36_report)
find_a4_frontier_candidates(baseline_payload, a36_report)
build_frontier_report(baseline_payload, a36_report)
assert_no_forbidden_semantic_keys(value, path="root")
```

Parser inputs:

```text
--run-dir                 required
--a36-report              required
--baseline-motion-graph   default vehicle_feasible_motion_graph.yaml
--pretty                  optional
```

The future report must not expose route-ready, start-reachability or optimality semantics.

## 7. Frontier Candidate Semantics

An A3.7 frontier candidate is a currently A3.6-REJECTED connector for which at least one of these is true:

```text
candidate -> existing A3.6 EXECUTABLE
existing A3.6 EXECUTABLE -> candidate
```

and the hypothetical two-transition chain has:

```text
continuous service-state boundary
3 distinct segment_ids
>= 2 distinct aisle_ids
```

Candidates must be deduplicated by `candidate_connector_id`.

Each candidate may contain multiple deterministic `hypothetical_witnesses` if it can pair with more than one current executable transition.

Witness position values are frozen as:

```text
PREDECESSOR_TO_EXECUTABLE
SUCCESSOR_FROM_EXECUTABLE
```

## 8. Tests-Only RED Commit

Tests-only commit:

```text
42b2c9a9dc0bd4034acc0a36ebf0342960044695
test(v25-12g): define A3.7 chain-frontier RED contract
```

Created only:

```text
tests/test_v25_12g_a37_chain_frontier_diagnosis_contract.py
```

No A3.7 production/harness file exists yet.

The test contract covers:

```text
read-only parser and source-level no-re-derivation rule
A3.6/A3.5 evidence overlay without baseline mutation
strict evidence provenance separation
predecessor frontier detection
successor frontier detection
3-segment / 2-aisle frontier gate
current outcome/frontier summary accounting
A3.6 integrity and connector-universe fail-closed behavior
recursive forbidden semantic-key rejection
```

## 9. Exact Next Action — Target-Machine G1 RED

First sync safely:

```bash
cd ~/agt_navigation_v2

git status --short

git fetch origin feat/v25-12g-maximum-feasible-coverage

git merge --ff-only origin/feat/v25-12g-maximum-feasible-coverage

git rev-parse HEAD
git status --short
```

Then run exactly:

```bash
source /opt/ros/humble/setup.bash
[ -f install/setup.bash ] && source install/setup.bash

PYTHONPATH="$PWD/src/agt_offline_assets:${PYTHONPATH:-}" \
python3 -m pytest -q \
  tests/test_v25_12g_a37_chain_frontier_diagnosis_contract.py \
  -k a37
```

## 10. Intended RED

With no A3.7 harness implemented, approximately 10 selected test instances are expected to fail because:

```text
tools/v25_12g_a37_chain_frontier_diagnosis.py
```

is missing.

A VALID RED has:

```text
selected tests > 0
passed = 0
failed = selected
errors = 0
all failures attributable to missing A3.7 harness / missing intended A3.7 API
```

Invalid RED examples:

```text
SyntaxError in the test file
fixture construction error
pytest collection error
unrelated import failure
failure caused by modifying protected local files
```

## 11. Stop at G1

After the RED command, stop and report:

```text
HEAD
collected
selected
passed
failed
errors
deselected
failure-family summary
protected-path git status
staged files
```

Do not create `tools/v25_12g_a37_chain_frontier_diagnosis.py` until G1 RED is reviewed and accepted.

Do not run the A3.6 real-data derivation again.

## 12. Later GREEN Scope — Only After Valid RED

After valid G1, the only new implementation file allowed is:

```text
tools/v25_12g_a37_chain_frontier_diagnosis.py
```

A3.7 must not modify:

```text
reverse_primitive_connector.py
reverse_primitive_diagnostics.py
vehicle_feasible_motion_graph.py
vehicle_feasible_transition_motion.py
motion-graph schema
A1/A2/runtime map assets
```

The real A3.7 analysis after GREEN is cheap and read-only: it reads the already frozen A3.6 JSON exactly as saved and does not re-run planning.

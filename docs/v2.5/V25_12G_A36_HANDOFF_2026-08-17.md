# V25-12G A3.6 Target-Machine RED Handoff

Date: 2026-08-17

This document is the compact continuation context for the next local Codex / ChatGPT session.

## 1. Repository / Branch

```text
Repository: Aldoubt/agt_navigation_v2
Branch: feat/v25-12g-maximum-feasible-coverage
ROS: ROS2 Humble
Workspace: ~/agt_navigation_v2
```

Use the original checkout only. Do not create another worktree.

## 2. Frozen A3.5 Baseline

A3.5 diagnostic production HEAD used for the real-data diagnosis:

```text
692b88cce215dba25df5952c89fdb49258e68687
feat(v25-12g): persist A3.5 connector diagnostics
```

A3.5 evidence freeze:

```text
cb0935bb23ba6981d3c6d06067f2417267981d9a
docs(v25-12g): freeze A3.5 connector diagnosis evidence
```

Formal A3.5 evidence:

```text
docs/v2.5/V25_12G_A35_CONNECTOR_BACKEND_DIAGNOSIS_2026-08-17.md
```

Frozen real result:

```text
40 R6B connectors
0 EXECUTABLE
40 REJECTED
40 queue exhausted
0 expansion-budget reached
behavior_projection_equal=True
final_transition_statuses_unchanged=True
protected_input_hashes_unchanged=True
forbidden_semantic_key_count=0
```

Failure classes:

```text
MIXED_LIMITATION           26
INCONCLUSIVE                6
FOOTPRINT_GRID_LIMITED      3
GOAL_CONNECTION_LIMITED     3
SITE_BOUNDARY_LIMITED       2
```

A4 is still blocked.

## 3. Approved A3.6 Amendment

User-approved design:

```text
A3.6 Intermediate-Curvature Primitive Amendment
```

The only intended planner behavior change is:

```text
before:
{-1.0/R, 0.0, +1.0/R}

after:
{-1.0/R, -0.5/R, 0.0, +0.5/R, +1.0/R}
```

`0.5` is fixed for this experiment. No parameter sweep is allowed.

Formal written spec:

```text
docs/superpowers/specs/2026-08-17-v25-12g-a36-intermediate-curvature-primitive-design.md
```

Spec commit:

```text
53e675cad28132a208fd7984804957d5c1574299
docs(v25-12g): design A3.6 intermediate-curvature primitive amendment
```

The user has explicitly approved the written spec (`spec认可`).

## 4. Strict TDD Implementation Plan

Plan:

```text
docs/superpowers/plans/2026-08-17-v25-12g-a36-intermediate-curvature-primitive.md
```

Plan commit:

```text
e3587ccc51810c68ae27fd243bab269690a93873
docs(v25-12g): plan A3.6 intermediate-curvature TDD
```

The plan is self-reviewed for spec coverage, placeholders and type/name consistency.

## 5. Current Stage: Tests-Only RED Landed

No A3.6 production behavior has been implemented yet.

Tests-only commits:

```text
113edc1114694ca9ce1929b26a61ad7f9a3022ea
test(v25-12g): add A3.6 primitive RED cases

79f177fb2dd255b7dc9ffb69e8c29f5132e02b6e
test(v25-12g): define A3.6 acceptance RED contract
```

Relative to plan HEAD `e3587ccc...`, the tests-only diff contains exactly:

```text
src/agt_offline_assets/test/test_reverse_primitive_connector.py
  +108 / -0

tests/test_v25_12g_a36_intermediate_curvature_contract.py
  +166 / -0
```

No production file, acceptance harness, motion-graph schema, runtime/map asset or protected path is changed by the RED batch.

## 6. RED Contract

Package test additions cover:

```text
1. fixed curvature fractions == (-1.0, -0.5, 0.0, 0.5, 1.0)
2. helper output == fraction / R
3. abs(curvature) <= 1/R
4. invalid radius fail-closed
5. one-primitive +0.5/R reachable-state fixture
6. five-category path-length diagnostic accounting
```

The synthetic reachable-state fixture uses:

```text
R = 1.5 m
primitive length = 0.30 m
goal = exact endpoint/yaw of +0.5/R primitive
position tolerance = 0.005 m
yaw tolerance = 1 deg
goal-shot distance = 0.01 m
max cusps = 1
max path length = 0.31 m
FREE navigation grid
legal non-limiting Site Boundary
```

The old three-curvature search is expected to remain unable to satisfy this fixture in one primitive.

Root contract additions require the future read-only harness:

```text
tools/v25_12g_a36_intermediate_curvature_acceptance.py
```

with contracts for:

```text
read-only parser
32 service-action preservation
40 connector-universe preservation
transition outcome comparison
A4 continuous two-transition / three-segment / two-aisle predicate
recursive forbidden semantic-key rejection
```

## 7. Exact Next Action: Local G1 RED

First sync safely:

```bash
cd ~/agt_navigation_v2
git fetch origin feat/v25-12g-maximum-feasible-coverage
git merge --ff-only origin/feat/v25-12g-maximum-feasible-coverage
```

Then run exactly:

```bash
source /opt/ros/humble/setup.bash
[ -f install/setup.bash ] && source install/setup.bash

PYTHONPATH="$PWD/src/agt_offline_assets:${PYTHONPATH:-}" \
python3 -m pytest -q \
  src/agt_offline_assets/test/test_reverse_primitive_connector.py \
  tests/test_v25_12g_a36_intermediate_curvature_contract.py \
  -k a36
```

Do not implement production before interpreting this output.

## 8. Valid RED Criteria

A valid G1 RED must have:

```text
no SyntaxError
no module import error caused by the tests
no fixture-construction error
no invalid vehicle/profile/admission error
```

Intended failure families are:

```text
A. missing _PRIMITIVE_CURVATURE_FRACTIONS
B. missing _primitive_curvature_values
C. synthetic +0.5/R goal remains NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION
D. path-length diagnostic accounting is still based on 3 primitive categories
E. A3.6 acceptance harness file is absent
```

With the current parametrization, approximately 16 A3.6 test instances are expected to be selected. Exact failure count is less important than every failure belonging to the intended families above.

If the synthetic reachability fixture fails because of Site Boundary, Navigation Grid, invalid config, R6A admission or goal-shot shortcut instead of the missing intermediate curvature, classify the RED as invalid and stop before production.

## 9. What Happens Only After Valid RED

After the operator reports a valid G1 RED, implement only:

```text
src/agt_offline_assets/agt_offline_assets/reverse_primitive_connector.py
```

Production changes are limited to:

```text
_PRIMITIVE_CURVATURE_FRACTIONS = (-1.0, -0.5, 0.0, 0.5, 1.0)
_primitive_curvature_values(radius)
search enumeration start=-2
path-length accounting derives len(curvature_values)
```

Do not modify:

```text
vehicle_feasible_motion_graph.py
vehicle_feasible_transition_motion.py
vehicle_feasible_motion_graph_io.py
reverse_primitive_diagnostics.py
CMakeLists.txt
```

unless a demonstrated contract defect forces a separately reviewed scope expansion.

After the primitive GREEN, create the read-only A3.6 harness required by the root RED contract.

## 10. Frozen Constraints During A3.6

All remain unchanged:

```text
mk_mini geometry
Ackermann kinematics
minimum turning radius = 1.50 m
canonical footprint
preview padding = 0.05 m
Site Boundary semantics
Navigation Grid FREE-only semantics
primitive length = 0.30 m
collision sampling = 0.10 m
state resolution = 0.15 m / 15 deg
goal tolerance = 0.18 m / 12 deg (production default)
goal-shot = forward-only Dubins, 3.0 m
max cusps = 2
max expansions = 30000
max path length = 18.0 m
search envelope padding = 0.80 m / 1.25 m
heuristic / queue ordering / cost model / dominance rule
A1 assets
A2 assets
motion-graph schema
```

No primitive may satisfy:

```text
abs(curvature) > 1/R
```

## 11. Protected Local Paths

Never touch, stage, reset, delete or rename:

```text
M  tools/rosbag_sensor_trimmer
?? src/agt_route_benchmark/
?? tools/map_tools/render_pcd_top_views.py
```

Do not use:

```text
git add .
git add -A
git clean
git reset --hard
git restore .
git checkout -- .
```

## 12. Later GREEN / Real-Data Gates

After valid G1:

```text
G2
  focused A3.6 GREEN
  existing A3.5 unit/contract regression
  colcon build agt_offline_assets
  package CTest

G3
  one and only one all-40 A3.6 acceptance run
  positive / partial / negative results are all valid if integrity gates pass
```

A3.6 real-data RC=0 does not require newly executable connectors.

If all 40 remain rejected, freeze the negative result. Do not add `±0.25/R`, increase path length, change state resolution or alter goal behavior inside A3.6.

## 13. A4 Entry Gate

A4 remains blocked unless the amended real graph contains:

```text
segment A
 -> EXECUTABLE transition 1
segment B
 -> EXECUTABLE transition 2
segment C
```

with:

```text
>= 3 distinct segment_ids
>= 2 distinct aisle_ids
>= 2 continuous EXECUTABLE inter-segment transitions
```

This is an engineering entry predicate only, not route-ready, optimal, field-ready or global-connectivity evidence.

# V25-12G A3.6 Frozen Handoff

Date: 2026-08-18

Use this document as the compact continuation context after the completed V25-12G A3.6 Intermediate-Curvature Primitive Amendment experiment.

## 1. Repository / Branch

```text
Repository: Aldoubt/agt_navigation_v2
Branch: feat/v25-12g-maximum-feasible-coverage
ROS: ROS2 Humble
Workspace: ~/agt_navigation_v2
```

Use the original checkout only. Do not create another worktree.

Protected unrelated local paths remain:

```text
M  tools/rosbag_sensor_trimmer
?? src/agt_route_benchmark/
?? tools/map_tools/render_pcd_top_views.py
```

Never modify, reset, delete, rename, restore or stage them. Never use `git add .`, `git add -A`, `git clean`, `git reset --hard`, `git restore .`, or `git checkout -- .`.

## 2. Frozen Stage Summary

```text
A1: PASS
A2: PASS
A3 local service actions: PASS
A3 inter-segment connectivity before A3.6: 0 / 40
A3.5 diagnostic instrumentation: PASS
A3.5 all-40 diagnosis: PASS / FROZEN
A3.6 G1: VALID RED
A3.6 G2: PASS
A3.6 G3: integrity PASS / executed exactly once
A3.6 real result: PARTIAL
A3.6 amended connectivity: 2 EXECUTABLE / 38 REJECTED
A4 minimum-chain predicate: False
A4: BLOCKED
```

Formal A3.6 evidence freeze:

```text
docs/v2.5/V25_12G_A36_INTERMEDIATE_CURVATURE_2026-08-18.md
```

Evidence freeze commit:

```text
434f5ef7237d0c47083a167531f84d1b1db0ff63
docs(v25-12g): freeze A3.6 intermediate-curvature evidence
```

## 3. Frozen A3.5 Baseline

Diagnostic implementation:

```text
692b88cce215dba25df5952c89fdb49258e68687
feat(v25-12g): persist A3.5 connector diagnostics
```

Evidence freeze:

```text
cb0935bb23ba6981d3c6d06067f2417267981d9a
docs(v25-12g): freeze A3.5 connector diagnosis evidence
```

Formal A3.5 evidence:

```text
docs/v2.5/V25_12G_A35_CONNECTOR_BACKEND_DIAGNOSIS_2026-08-17.md
```

Frozen A3.5 real result:

```text
40 R6B connectors
0 EXECUTABLE
40 REJECTED
40 queue exhausted
0 expansion-budget reached
```

A3.5 did not support increasing the 30,000 expansion cap as the first response.

## 4. Approved A3.6 Amendment

Formal spec:

```text
docs/superpowers/specs/2026-08-17-v25-12g-a36-intermediate-curvature-primitive-design.md
```

Implementation plan:

```text
docs/superpowers/plans/2026-08-17-v25-12g-a36-intermediate-curvature-primitive.md
```

Only approved planner behavior change:

```text
before: {-1.0/R, 0.0, +1.0/R}
after:  {-1.0/R, -0.5/R, 0.0, +0.5/R, +1.0/R}
```

The `0.5` fraction is fixed. No curvature parameter sweep was permitted.

Production commit:

```text
13031ee094a0557d35195d0100cfca823e2f73bf
feat(v25-12g): add intermediate R6B curvature primitives
```

Acceptance harness commit:

```text
a1f8d77719d155962ae08b7156f64c4292b7c54e
feat(v25-12g): add A3.6 intermediate-curvature acceptance harness
```

Harness:

```text
tools/v25_12g_a36_intermediate_curvature_acceptance.py
```

The harness is read-only with respect to runtime/map assets.

## 5. Frozen Constraints

A3.6 did not change:

```text
mk_mini geometry
Ackermann kinematics
minimum turning radius = 1.50 m
canonical footprint
preview padding = 0.05 m
Site Boundary semantics
Navigation Grid FREE-only semantics
primitive length = 0.30 m
collision sampling = 0.10 m
state resolution = 0.15 m / 15 deg
goal tolerance = 0.18 m / 12 deg
goal-shot = forward-only Dubins, 3.0 m
max cusps = 2
max expansions = 30000
max path length = 18.0 m
search envelope padding = 0.80 m / 1.25 m
heuristic / queue ordering / cost model / dominance rule
A1 assets
A2 assets
motion-graph schema
```

No primitive exceeds `abs(curvature) > 1/R`.

## 6. G1 VALID RED

Tests-only RED HEAD:

```text
c2af3784c78a000a8902dde3795f6a361f07811f
```

Target-machine evidence:

```text
Collected:  26
Selected:   16
Passed:      0
Failed:     16
Errors:      0
Deselected: 10
```

Failure families:

```text
curvature API missing                          6
synthetic +0.5/R reachable-state failure      1
five-primitive path-length accounting         1
A3.6 acceptance harness missing               8
```

Synthetic status before production:

```text
NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION
```

Pre-amendment path-length accounting:

```text
search_expansions = 3
primitive_edges_considered = 9
primitive_edges_rejected_path_length = 9
```

G1 was accepted as VALID RED.

## 7. G2 PASS

Focused A3.6:

```text
Collected:  26
Selected:   16
Passed:     16
Failed:      0
Errors:      0
Deselected: 10
```

Synthetic `+0.5/R` result:

```text
status = FORWARD_PRIMITIVE_PREVIEW_FREE
path_length_m = 0.29998611130401104
goal_position_error_m = 5.55e-17
goal_yaw_error_rad = 4.44e-16
contains +0.5/R curvature sample = True
```

Five-category accounting:

```text
search_expansions = 3
primitive_edges_considered = 15
primitive_edges_rejected_path_length = 15
```

A3.5 regression subset:

```text
89 / 89 passed
0 failed
0 errors
```

Build / package:

```text
agt_offline_assets build: PASS
CTest: 43 / 43 passed
failed: 0
skipped: 0
```

Frozen parameter check:

```text
fractions = (-1.0, -0.5, 0.0, 0.5, 1.0)
max curvature = 0.6666666666666666
1/R limit = 0.6666666666666666
A36_FROZEN_PARAMETER_CHECK=PASS
```

## 8. G3 — Executed Exactly Once

Target-machine G3 HEAD:

```text
d6d35a0817ca33ead6e04970fcdd7f5ef3b231e7
```

Result:

```text
A36_RC = 0
real-data derivation count = exactly once
```

Saved target-machine evidence:

```text
/tmp/v25_12g_a36_intermediate_curvature.json
/tmp/v25_12g_a36_intermediate_curvature.time
```

Do not rerun A3.6 merely to improve or reconfirm the result.

## 9. G3 Real Result — PARTIAL

Baseline:

```text
EXECUTABLE:  0
REJECTED:   40
```

A3.6 amended:

```text
EXECUTABLE:  2
REJECTED:   38
```

Backend status:

```text
REVERSE_PRIMITIVE_PREVIEW_FREE:          2
NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION:  38
```

Operator-supplied abbreviated recovered connector descriptions:

```text
...aisle_016...to.aisle_017...dead_end_forward_in_reverse_out
...aisle_016...to.aisle_017...service_high_to_low
```

These are abbreviations, not canonical full IDs. The exact full recovered IDs and all 38 rejected IDs remain in the saved target-machine JSON. Do not reconstruct missing identifier text from naming conventions.

## 10. G3 Integrity PASS

```text
service_behavior_equal = True
connector_universe_preserved = True
protected_input_hashes_unchanged = True
forbidden_semantic_key_count = 0
unexpectedly_regressed_connector_ids = []
connector_count = 40
```

Protected local paths remained untouched and unstaged; staged files were empty.

## 11. A3.6 Search / Resource Observation

Search termination:

```text
queue_exhausted_count = 35
expansion_budget_reached_count = 3
```

Operator-supplied histogram entries:

```text
0-9:           4
100-499:      16
500-999:       5
1000-4999:     5
10000-19999:   5
20000-30000:   5
```

The `10-99` bin was not explicitly supplied and is not independently asserted.

Reported low-expansion failures included:

```text
3 expansions:       FOOTPRINT_GRID_LIMITED x2
7 / 9 expansions:   SITE_BOUNDARY_LIMITED
121-198 expansions: MIXED_LIMITATION examples
```

Unlike A3.5, A3.6 reaches the frozen 30,000-expansion cap for 3 connectors. This is a legitimate result of the amended search workload, not permission to raise the cap inside A3.6.

Resource evidence:

```text
harness wall: 3066.34 s
external elapsed: 51:06.61
harness user: 3064.05 s
external user: 3065.64 s
harness system: 0.295 s
external system: 2.50 s
CPU: about 100%
max RSS: 93748 kB
```

For comparison, frozen A3.5 all-40 diagnosis was approximately `20:06.31` elapsed with `82908 kB` maximum RSS. The five-category lattice therefore has a materially higher observed compute cost on this frozen dataset.

## 12. A4 Gate

G3 reported:

```text
a4_entry_gate_passed = False
witness = empty
```

A4 still requires:

```text
segment A
 -> EXECUTABLE transition 1
segment B
 -> EXECUTABLE transition 2
segment C
```

with:

```text
>= 3 distinct segment_ids
>= 2 distinct aisle_ids
>= 2 continuous EXECUTABLE inter-segment transitions
```

The two recovered A3.6 connectors do not satisfy this frozen predicate.

```text
A4 = BLOCKED
```

This is not a global infeasibility claim.

## 13. Current Development Boundary

A3.6 is complete and frozen with result:

```text
PARTIAL
```

Meaning:

```text
intermediate curvature recovered 2 real connector transitions
but
A4 minimum-chain predicate remains false
```

Do not continue A3.6 by:

```text
adding ±0.25/R
raising max_expansions
raising max_path_length
changing state resolution
relaxing goal tolerance
changing Site Boundary / Grid semantics
adding Reeds-Shepp
stacking any second backend amendment
```

If connector-backend development continues, it must begin with a **new evidence-backed brainstorm/spec cycle**. This handoff intentionally does not choose that next amendment.

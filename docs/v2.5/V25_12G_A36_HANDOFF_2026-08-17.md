# V25-12G A3.6 Target-Machine G3 Handoff

Date: 2026-08-18

This is the compact continuation context for the one-and-only-one real-data A3.6 experiment.

## 1. Repository / Branch

```text
Repository: Aldoubt/agt_navigation_v2
Branch: feat/v25-12g-maximum-feasible-coverage
ROS: ROS2 Humble
Workspace: ~/agt_navigation_v2
```

Use the original checkout only. Do not create another worktree.

Protected unrelated local paths remain:

```text
M  tools/rosbag_sensor_trimmer
?? src/agt_route_benchmark/
?? tools/map_tools/render_pcd_top_views.py
```

Never modify, reset, delete, rename, restore or stage them. Never use `git add .`, `git add -A`, `git clean`, `git reset --hard`, `git restore .`, or `git checkout -- .`.

## 2. Frozen A3.5 Baseline

Diagnostic implementation HEAD:

```text
692b88cce215dba25df5952c89fdb49258e68687
feat(v25-12g): persist A3.5 connector diagnostics
```

Evidence freeze:

```text
cb0935bb23ba6981d3c6d06067f2417267981d9a
docs(v25-12g): freeze A3.5 connector diagnosis evidence
```

Formal evidence:

```text
docs/v2.5/V25_12G_A35_CONNECTOR_BACKEND_DIAGNOSIS_2026-08-17.md
```

Frozen real result:

```text
40 R6B connectors
0 EXECUTABLE
40 REJECTED
40 queue exhausted
0 expansion-budget reached
```

A4 remains blocked until the minimum continuous-chain predicate is satisfied.

## 3. Approved A3.6 Amendment

Formal spec:

```text
docs/superpowers/specs/2026-08-17-v25-12g-a36-intermediate-curvature-primitive-design.md
```

Implementation plan:

```text
docs/superpowers/plans/2026-08-17-v25-12g-a36-intermediate-curvature-primitive.md
```

Only approved planner behavior change:

```text
before: {-1.0/R, 0.0, +1.0/R}
after:  {-1.0/R, -0.5/R, 0.0, +0.5/R, +1.0/R}
```

`0.5` is fixed. No parameter sweep and no second amendment are allowed inside A3.6.

Frozen constraints remain unchanged:

```text
minimum turning radius = 1.50 m
canonical footprint unchanged
preview padding = 0.05 m
Site Boundary unchanged
Navigation Grid FREE-only unchanged
primitive length = 0.30 m
collision sampling = 0.10 m
state resolution = 0.15 m / 15 deg
goal tolerance = 0.18 m / 12 deg
goal shot = forward-only Dubins, 3.0 m
max cusps = 2
max expansions = 30000
max path length = 18.0 m
search envelope padding = 0.80 m / 1.25 m
heuristic / queue ordering / cost / dominance unchanged
A1 / A2 assets unchanged
motion-graph schema unchanged
```

## 4. G1 VALID RED

Tests-only RED HEAD:

```text
c2af3784c78a000a8902dde3795f6a361f07811f
```

Target-machine evidence:

```text
Collected: 26
Selected: 16
Passed: 0
Failed: 16
Errors: 0
Deselected: 10
```

Failure families:

```text
curvature API missing                          6
synthetic +0.5/R reachable-state failure      1
five-primitive path-length accounting         1
A3.6 acceptance harness missing               8
```

Synthetic fixture actual result before production:

```text
NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION
```

Path-length accounting before production:

```text
search_expansions=3
primitive_edges_considered=9
primitive_edges_rejected_path_length=9
```

G1 was accepted as VALID RED with no unexpected failure family.

## 5. GREEN Candidate Code

Minimal production commit:

```text
13031ee094a0557d35195d0100cfca823e2f73bf
feat(v25-12g): add intermediate R6B curvature primitives
```

Read-only acceptance harness commit:

```text
a1f8d77719d155962ae08b7156f64c4292b7c54e
feat(v25-12g): add A3.6 intermediate-curvature acceptance harness
```

Harness:

```text
tools/v25_12g_a36_intermediate_curvature_acceptance.py
```

The harness re-derives the amended motion graph in memory, compares service behavior and the connector universe against the frozen A3.5 baseline, allows transition improvement, rejects regressions, hashes protected inputs before/after, reports bounded-search counters, and computes the A4 minimum-chain predicate. It writes no runtime/map asset.

## 6. G2 PASS — Target-Machine Evidence

G2 was executed on the target machine and accepted as PASS.

### 6.1. Focused A3.6

```text
Collected: 26
Selected: 16
Passed: 16
Failed: 0
Errors: 0
Deselected: 10
```

Synthetic `+0.5/R` fixture:

```text
status = FORWARD_PRIMITIVE_PREVIEW_FREE
path_length_m = 0.29998611130401104
goal_position_error_m = 5.55e-17
goal_yaw_error_rad = 4.44e-16
contains +0.5/R curvature sample = True
```

Five-primitive accounting:

```text
search_expansions = 3
primitive_edges_considered = 15
primitive_edges_rejected_path_length = 15
15 = 5 * 3
```

### 6.2. A3.5 regression subset

```text
Collected: 89
Passed: 89
Failed: 0
Errors: 0
```

### 6.3. Build / package tests

```text
colcon build --packages-select agt_offline_assets --symlink-install: PASS
package CTest: 43 / 43 passed
failed: 0
skipped: 0
```

### 6.4. Frozen parameters

```text
fractions = (-1.0, -0.5, 0.0, 0.5, 1.0)
max curvature = 0.6666666666666666
1/R limit = 0.6666666666666666
A36_FROZEN_PARAMETER_CHECK=PASS
```

No protected path was staged or changed by A3.6.

G3 has not yet been executed.

## 7. Current Stage

```text
A3.5 diagnosis frozen
-> A3.6 spec approved
-> G1 VALID RED
-> minimal five-curvature amendment
-> read-only acceptance harness
-> G2 PASS
-> CURRENT: one-and-only-one all-40 G3 real-data experiment
```

Do not change code or planner parameters before G3.

## 8. Exact G3 Execution — Run Once

First safely synchronize and confirm status:

```bash
cd ~/agt_navigation_v2

git status --short

git fetch origin feat/v25-12g-maximum-feasible-coverage

git merge --ff-only origin/feat/v25-12g-maximum-feasible-coverage

git rev-parse HEAD
git status --short
```

If fast-forward is blocked by unexpected local commits/conflicts, stop. Do not reset.

Then prepare the target environment:

```bash
source /opt/ros/humble/setup.bash
source install/setup.bash
export PYTHONPATH="$PWD/src/agt_offline_assets:${PYTHONPATH:-}"
```

Remove only previous temporary output files:

```bash
rm -f \
  /tmp/v25_12g_a36_intermediate_curvature.json \
  /tmp/v25_12g_a36_intermediate_curvature.time
```

Run the real experiment exactly once:

```bash
/usr/bin/time -v \
python3 tools/v25_12g_a36_intermediate_curvature_acceptance.py \
  --run-dir runtime/maps/agt_workbench_run \
  --vehicle-profile profiles/platforms/mk_mini.yaml \
  --baseline-motion-graph vehicle_feasible_motion_graph.yaml \
  --pretty \
  > /tmp/v25_12g_a36_intermediate_curvature.json \
  2> /tmp/v25_12g_a36_intermediate_curvature.time

A36_RC=$?
echo "A36_RC=$A36_RC"
```

Do not run the harness a second time merely because connectivity did not improve.

If `A36_RC != 0`, stop and preserve both `/tmp` files. Do not tune or rerun. Report the error/timing stream for review.

## 9. Inspect Saved JSON Only — No Re-Derivation

If `A36_RC=0`, inspect the already-saved JSON without rerunning the planner:

```bash
python3 - <<'PY'
import json
from pathlib import Path

path = Path('/tmp/v25_12g_a36_intermediate_curvature.json')
data = json.loads(path.read_text(encoding='utf-8'))
s = data['summary']

print('schema =', data['schema'])
print('validation_scope =', data['validation_scope'])
print('baseline_transition_status_counts =', s['baseline_transition_status_counts'])
print('amended_transition_status_counts =', s['amended_transition_status_counts'])
print('newly_executable_connector_ids =', s['newly_executable_connector_ids'])
print('newly_executable_count =', len(s['newly_executable_connector_ids']))
print('still_rejected_count =', len(s['still_rejected_connector_ids']))
print('still_rejected_connector_ids =', s['still_rejected_connector_ids'])
print('unexpectedly_regressed_connector_ids =', s['unexpectedly_regressed_connector_ids'])
print('queue_exhausted_count =', s['queue_exhausted_count'])
print('expansion_budget_reached_count =', s['expansion_budget_reached_count'])
print('expansion_histogram =', s['expansion_histogram'])
print('service_behavior_equal =', s['service_behavior_equal'])
print('connector_universe_preserved =', s['connector_universe_preserved'])
print('protected_input_hashes_unchanged =', s['protected_input_hashes_unchanged'])
print('forbidden_semantic_key_count =', s['forbidden_semantic_key_count'])
print('a4_minimum_chain =', s['a4_minimum_chain'])
print('wall_seconds =', s['wall_seconds'])
print('user_seconds =', s['user_seconds'])
print('system_seconds =', s['system_seconds'])
print('cpu_percent_approx =', s['cpu_percent_approx'])
print('max_rss_kb =', s['max_rss_kb'])
PY
```

Then inspect external process metrics:

```bash
grep -E \
'Elapsed|User time|System time|Maximum resident set size|Percent of CPU' \
/tmp/v25_12g_a36_intermediate_curvature.time
```

Finally confirm local protected paths remain unchanged and unstaged:

```bash
git status --short
git diff --cached --name-only
```

## 10. G3 Integrity Criteria

A valid `A36_RC=0` experiment requires:

```text
service_behavior_equal = True
connector_universe_preserved = True
protected_input_hashes_unchanged = True
forbidden_semantic_key_count = 0
unexpectedly_regressed_connector_ids = []
40 connector transitions remain represented
```

Connectivity improvement is not required for experiment validity.

Valid result families:

```text
NEGATIVE: 0 newly executable connectors
PARTIAL: >=1 newly executable connector but A4 minimum chain absent
A4-CANDIDATE: A4 minimum-chain predicate true
```

Do not hide or rerun a NEGATIVE result.

## 11. A4 Entry Predicate

A4 remains blocked unless `summary.a4_minimum_chain.a4_entry_gate_passed` is `True` with a witness containing:

```text
2 continuous EXECUTABLE transitions
3 distinct segment IDs
>= 2 distinct aisle IDs
```

This is only an engineering entry predicate. It is not route-ready, optimal, field-ready or global-connectivity evidence.

## 12. Stop After G3

After the one real-data run, stop and report the saved evidence. Do not modify planner code, add `±0.25/R`, change path length/state resolution/goal behavior, or start A4 automatically.

The next repository action after G3 review is evidence freeze only:

```text
docs/v2.5/V25_12G_A36_INTERMEDIATE_CURVATURE_2026-08-18.md
```

and handoff alignment. Any further backend amendment requires a new brainstorm/spec cycle.

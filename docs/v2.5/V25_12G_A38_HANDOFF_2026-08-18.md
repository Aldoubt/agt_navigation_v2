# V25-12G A3.8 Selective A4-Frontier Current Diagnostics RED Handoff

Date: 2026-08-18

Status: **A3.6 FROZEN / A4 BLOCKED / A3.7 FRONTIER OBSERVED / A3.8 SELECTIVE-6 TESTS-ONLY RED LANDED**

Stage:

```text
V25-12G A3.8 — Selective A4-Frontier Current Diagnostics
```

A3.8 is diagnostic-only. It reruns the current five-curvature R6B pipeline only for the six A3.7 A4-frontier connectors. It does not amend planner behavior and it does not rerun all 40 connectors.

## 1. Repository / Branch

```text
Repository: Aldoubt/agt_navigation_v2
Branch: feat/v25-12g-maximum-feasible-coverage
Workspace: ~/agt_navigation_v2
ROS: ROS2 Humble
```

Use the original checkout only. Do not create another worktree.

Protected unrelated local paths remain:

```text
M  tools/rosbag_sensor_trimmer
?? src/agt_route_benchmark/
?? tools/map_tools/render_pcd_top_views.py
```

Never modify, reset, delete, rename, restore or stage them.

Do not use:

```text
git add .
git add -A
git clean
git reset --hard
git restore .
git checkout -- .
```

## 2. Why A3.8 Pivoted

The initial A3.8 evidence-join design expected the frozen A3.5 G3 JSON at:

```text
/tmp/v25_12g_a35_connector_diagnosis.json
```

Target-machine recovery checks established:

```text
A35_JSON_EXISTS=1
A37_JSON_EXISTS=0
```

A home/repository search found no recoverable A3.5 diagnostic report. The only matching JSON was unrelated CMake File API metadata.

Therefore the old evidence-join RED contract was not executed and is superseded.

A3.8 now targets the evidence actually needed for the next decision: detailed diagnostics under the current A3.6 five-curvature backend, but only for the six connectors that can complete the A4 minimum chain if one is recovered.

## 3. Frozen A3.7 Frontier

Saved A3.7 report:

```text
/tmp/v25_12g_a37_chain_frontier.json
```

Observed frontier count:

```text
6
```

All six are currently:

```text
status = REJECTED
backend_status = NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION
expansion_budget_reached = False
```

The six frontier connectors are the only connectors in A3.8 scope.

A3.8 must not broaden the experiment back to 40 connectors.

## 4. Selective Execution Design

The future harness will load the canonical real inputs needed by the existing transition validator:

```text
vehicle_feasible_service_graph.yaml
turn_zones.yaml
navigation_map.yaml
navigation_map.pgm
site_boundary.yaml
canonical mk_mini vehicle profile
```

It will load the A3.7 report and extract exactly the six `frontier_candidate_ids`.

It will then select exactly those six `ConnectorCandidate` objects from the original A2 service graph and create an in-memory service-graph view using `dataclasses.replace`:

```text
full service graph with 40 connector candidates
        ↓ select A3.7 frontier IDs only
in-memory service graph with 6 connector candidates
        ↓
validate_transition_candidates(...)
```

The production `validate_transition_candidates()` function is not modified.

Because that function derives requests only from `service_graph.connector_candidates`, the selective in-memory graph causes forward gate -> forward audit -> R6A -> R6B to operate only on the six selected requests.

## 5. No Planner Amendment

A3.8 must not change:

```text
five curvature fractions {-1, -0.5, 0, +0.5, +1}/R
minimum turning radius 1.5 m
primitive length 0.30 m
collision sample step 0.10 m
state resolution 0.15 m / 15 deg
goal tolerance 0.18 m / 12 deg
goal-shot distance 3.0 m
max cusps 2
max expansions 30000
max path length 18.0 m
search-envelope padding 0.80 / 1.25 m
preview footprint padding 0.05 m
Site Boundary semantics
Navigation Grid FREE-only semantics
footprint geometry
heuristic / dominance / penalty semantics
```

A3.8 must not call:

```text
derive_vehicle_feasible_motion_graph
```

A3.8 must not call `derive_reverse_primitive_connector_plan` directly. It must use the existing transition-validation chain so forward audit and R6A semantics remain identical to A3.6.

## 6. Current-Outcome Integrity Gate

A3.7 already preserves the A3.6 current evidence for every frontier connector:

```text
status
backend_status
search_expansions
queue_exhausted
expansion_budget_reached
failure_class
```

The selective rerun must reproduce those six fields exactly for all six connector IDs.

Any mismatch is:

```text
A3.8 current outcome drift
```

and the harness must fail closed rather than interpret the new counters.

This protects the detailed diagnostic report from silently becoming a different experiment.

## 7. Diagnostic Output

For each frontier connector the selective report must retain two separate namespaces:

```text
a37_frontier
    saved A3.6 outcome evidence + A3.7 topology/witness

current_evidence
    rerun status/backend/termination/failure-class fields

current_reverse_backend_diagnostics
    complete current five-curvature R6B diagnostic mapping
```

The detailed mapping includes the existing A3.5-instrumented fields now produced under the current A3.6 five-curvature search:

```text
start_goal_position_error_m
best_goal_position_error_m
best_goal_yaw_error_rad
best_goal_distance_state_direction
best_goal_distance_cusp_count

nodes_popped
stale_nodes_skipped

cusp_switches_considered
cusp_switches_rejected_state_dominance
cusp_switches_enqueued
nodes_at_max_cusps

primitive_edges_considered
primitive_edges_rejected_search_envelope
primitive_edges_rejected_site_boundary
primitive_edges_rejected_navigation_grid
primitive_edges_rejected_path_length
primitive_edges_rejected_state_dominance
primitive_edges_enqueued

goal_tolerance_checks
goal_tolerance_successes

goal_shot_attempts
goal_shot_reverse_direction_blocked
goal_shot_candidates_considered
goal_shot_rejected_path_length
goal_shot_rejected_search_envelope
goal_shot_rejected_site_boundary
goal_shot_rejected_navigation_grid
goal_shot_successes
```

## 8. Descriptive Evidence Only

A3.8 may report deterministic counts/distributions across the six current diagnostic rows, but it must not synthesize causal or amendment conclusions.

Forbidden semantic/decision keys remain:

```text
route_ready
reachable_from_start
optimal
root_cause
recommended_amendment
```

The next backend hypothesis is chosen only after inspecting the real selective counters.

## 9. Future Harness Contract

Future path:

```text
tools/v25_12g_a38_selective_frontier_diagnostics.py
```

Frozen constants:

```text
REPORT_SCHEMA = agt_v25_12g_a38_selective_frontier_diagnostics/v1
VALIDATION_SCOPE = A38_SELECTIVE_A4_FRONTIER_CURRENT_DIAGNOSTIC_ONLY_NOT_PLANNER_AMENDMENT_NOT_ROUTE_READY
A37_REPORT_SCHEMA = agt_v25_12g_a37_a4_chain_frontier_diagnosis/v1
EXPECTED_FRONTIER_COUNT = 6
```

Required helpers:

```text
build_parser()
frontier_connector_ids(a37_report)
select_frontier_connector_candidates(all_candidates, a37_report)
compare_selective_outcomes(a37_report, validations)
build_selective_report(a37_report, validations)
assert_no_forbidden_semantic_keys(value, path="root")
```

Parser inputs:

```text
--run-dir           required
--vehicle-profile   required
--a37-report        required
--service-graph     default vehicle_feasible_service_graph.yaml
--turn-zones        default turn_zones.yaml
--navigation-map    default navigation_map.yaml
--navigation-pgm    default navigation_map.pgm
--site-boundary     default site_boundary.yaml
--pretty            optional
```

No manual `--connector-ids` override is allowed. The six IDs must come from frozen A3.7 evidence.

No output/write flag is allowed. Report JSON goes to stdout.

## 10. Tests-Only RED

The superseded evidence-join test path is retained for repository-history continuity, but its contents have been fully replaced by the selective-current-diagnostics RED contract:

```text
tests/test_v25_12g_a38_frontier_diagnostic_evidence_join_contract.py
```

Selective RED commit:

```text
ec941bb047189c31ebce750d9c568d7a76b7b16f
test(v25-12g): pivot A3.8 RED to selective frontier diagnostics
```

The future harness does not exist yet.

## 11. Exact Target-Machine G1 RED Gate

Synchronize safely:

```bash
cd ~/agt_navigation_v2

git status --short

git fetch origin feat/v25-12g-maximum-feasible-coverage
git merge --ff-only origin/feat/v25-12g-maximum-feasible-coverage

git rev-parse HEAD
git status --short
```

Confirm only the still-required A3.7 report exists:

```bash
test -f /tmp/v25_12g_a37_chain_frontier.json
echo "A37_JSON_EXISTS=$?"
```

Expected:

```text
A37_JSON_EXISTS=0
```

Do not recreate or rerun A3.5.

Run only the A3.8 RED contract:

```bash
source /opt/ros/humble/setup.bash
[ -f install/setup.bash ] && source install/setup.bash

PYTHONPATH="$PWD/src/agt_offline_assets:${PYTHONPATH:-}" \
python3 -m pytest -q \
  tests/test_v25_12g_a38_frontier_diagnostic_evidence_join_contract.py \
  -k a38
```

Expected selected test instances:

```text
10
```

Valid RED requires:

```text
Passed: 0
Failed: 10
Errors: 0
```

Every failure must be caused by:

```text
missing A3.8 selective-frontier harness:
tools/v25_12g_a38_selective_frontier_diagnostics.py
```

Invalid RED examples:

```text
collection error
SyntaxError
fixture construction failure
unexpected import error
existing selective harness unexpectedly present
```

## 12. Stop After G1

After the RED command, stop and report. Do not implement the harness locally and do not run the six real connectors yet.

Report:

```text
HEAD
A37_JSON_EXISTS
Collected
Selected
Passed
Failed
Errors
Deselected
Failure family counts
git status --short
git diff --cached --name-only
```

## 13. After VALID RED

Only after operator-supplied VALID RED may production add:

```text
tools/v25_12g_a38_selective_frontier_diagnostics.py
```

Then the target machine will run synthetic GREEN first.

Only after GREEN will A3.8 execute one controlled selective real-data run of exactly six frontier connectors and save its report separately.

A3.8 does not rerun all 40 connectors and does not advance A4 by itself.

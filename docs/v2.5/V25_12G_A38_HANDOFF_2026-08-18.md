# V25-12G A3.8 Operator-Manifest Selective Current Diagnostics RED Handoff

Date: 2026-08-19

Status: **A3.6 FROZEN / A4 BLOCKED / A3.7 FRONTIER OBSERVED / ORIGINAL A3.5-A3.7 TMP ARTIFACTS LOST / A3.8 MANIFEST-DRIVEN TESTS-ONLY RED LANDED**

Stage:

```text
V25-12G A3.8 — Selective A4-Frontier Current Diagnostics
```

A3.8 is diagnostic-only. It will rerun the current five-curvature transition-validation pipeline only for the six operator-frozen A3.7 A4-frontier connectors. It does not amend planner behavior and it does not rerun all 40 connectors.

## 1. Repository / Branch

```text
Repository: Aldoubt/agt_navigation_v2
Branch: feat/v25-12g-maximum-feasible-coverage
Workspace: ~/agt_navigation_v2
ROS: ROS2 Humble
```

Use the original checkout only. Do not create another worktree.

Protected unrelated local paths remain:

```text
M  tools/rosbag_sensor_trimmer
?? src/agt_route_benchmark/
?? tools/map_tools/render_pcd_top_views.py
```

Never modify, reset, delete, rename, restore or stage them.

Do not use:

```text
git add .
git add -A
git clean
git reset --hard
git restore .
git checkout -- .
```

## 2. Artifact-Loss Recovery Decision

Target-machine checks established that the temporary experiment artifacts are no longer available:

```text
/tmp/v25_12g_a35_connector_diagnosis.json              missing
/tmp/v25_12g_a36_intermediate_curvature.json           missing
/tmp/v25_12g_a37_chain_frontier.json                    missing
```

A3.6 must not be rerun merely to recreate a temporary report. Because A3.7 was a read-only topology overlay over the now-missing A3.6 report, the original A3.7 JSON cannot be regenerated without violating the frozen A3.6 run policy.

However, the operator previously supplied the full A3.7 console frontier details. Those supplied records include all six frontier connector IDs, topology fields, A3.6 current outcome fields, and hypothetical A4 witnesses needed to define the selective diagnostic scope.

Therefore the approved recovery is an explicit repository-persisted operator frontier manifest.

## 3. Persistent Operator Frontier Manifest

Repository evidence path:

```text
docs/v2.5/evidence/V25_12G_A37_OPERATOR_FRONTIER_MANIFEST_2026-08-18.json
```

Manifest schema:

```text
agt_v25_12g_a37_operator_frontier_manifest/v1
```

Its provenance is explicitly:

```text
kind = OPERATOR_SUPPLIED_A37_CONSOLE_EVIDENCE
original_a35_json_available = false
original_a36_json_available = false
original_a37_json_available = false
reconstructed_from_console_output = true
reconstruction_policy = ONLY_FIELDS_EXPLICITLY_PRESENT_IN_OPERATOR_SUPPLIED_A37_CONSOLE_OUTPUT
```

The manifest is not represented as the original A3.7 JSON. It contains only fields explicitly present in the operator-supplied console evidence.

It intentionally does not copy the null A3.5 historical placeholders from the console output because those fields are not useful current diagnostic evidence.

## 4. Frozen Six-Connector Scope

The manifest contains exactly six frontier candidate IDs:

```text
1. headland.turn_high_u.aisle_017.segment_005.dead_end_forward_in_reverse_out.to.aisle_019.segment_003.dead_end_forward_in_reverse_out
2. headland.turn_high_u.aisle_017.segment_005.dead_end_forward_in_reverse_out.to.aisle_019.segment_003.service_high_to_low
3. headland.turn_high_u.aisle_017.segment_005.dead_end_forward_in_reverse_out.to.aisle_020.segment_001.service_high_to_low
4. headland.turn_high_u.aisle_019.segment_003.dead_end_forward_in_reverse_out.to.aisle_016.segment_003.dead_end_forward_in_reverse_out
5. headland.turn_high_u.aisle_019.segment_003.service_low_to_high.to.aisle_016.segment_003.dead_end_forward_in_reverse_out
6. headland.turn_high_u.aisle_020.segment_001.service_low_to_high.to.aisle_016.segment_003.dead_end_forward_in_reverse_out
```

Operator-frozen A3.6 outcomes:

```text
#1 REJECTED / NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION / 14828 / queue exhausted / MIXED_LIMITATION
#2 REJECTED / NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION / 14828 / queue exhausted / MIXED_LIMITATION
#3 REJECTED / NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION / 14912 / queue exhausted / MIXED_LIMITATION
#4 REJECTED / NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION /   202 / queue exhausted / INCONCLUSIVE
#5 REJECTED / NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION /   125 / queue exhausted / MIXED_LIMITATION
#6 REJECTED / NO_REVERSE_PRIMITIVE_PREVIEW_SOLUTION /   301 / queue exhausted / INCONCLUSIVE
```

All six have:

```text
expansion_budget_reached = false
```

The manifest also retains all explicit successor/predecessor A4 witnesses supplied by the operator.

## 5. Selective Execution Design

The future A3.8 harness will load canonical real inputs:

```text
vehicle_feasible_service_graph.yaml
turn_zones.yaml
navigation_map.yaml
navigation_map.pgm
site_boundary.yaml
canonical mk_mini vehicle profile
```

It will load the repository operator manifest and extract exactly the six frontier IDs.

It will select exactly those six `ConnectorCandidate` objects from the original A2 service graph and construct an in-memory view using `dataclasses.replace`:

```text
full service graph with 40 connector candidates
        ↓ exact manifest-ID selection
in-memory service graph with 6 connector candidates
        ↓
validate_transition_candidates(...)
```

The production `validate_transition_candidates()` function is not modified.

Because the existing transition validator derives requests from `service_graph.connector_candidates`, this in-memory view causes the normal forward gate -> forward audit -> R6A -> current five-curvature R6B chain to operate only on the six selected requests.

## 6. No Planner Amendment

A3.8 must not change:

```text
five curvature fractions {-1, -0.5, 0, +0.5, +1}/R
minimum turning radius 1.5 m
primitive length 0.30 m
collision sample step 0.10 m
state resolution 0.15 m / 15 deg
goal tolerance 0.18 m / 12 deg
goal-shot distance 3.0 m
max cusps 2
max expansions 30000
max path length 18.0 m
search-envelope padding 0.80 / 1.25 m
preview footprint padding 0.05 m
Site Boundary semantics
Navigation Grid FREE-only semantics
canonical footprint
heuristic / queue / reverse cost / cusp penalty / steering penalty / dominance semantics
```

A3.8 harness must not call:

```text
derive_vehicle_feasible_motion_graph
```

It must not call `derive_reverse_primitive_connector_plan` directly. It must use `validate_transition_candidates()` so forward audit and R6A semantics stay on the existing production chain.

## 7. Manifest Integrity Gate

The harness must fail closed unless all are true:

```text
schema = agt_v25_12g_a37_operator_frontier_manifest/v1
provenance.kind = OPERATOR_SUPPLIED_A37_CONSOLE_EVIDENCE
provenance.reconstructed_from_console_output = true
original_a35_json_available = false
original_a36_json_available = false
original_a37_json_available = false
frontier_candidate_count = 6
current_a4_entry_gate_passed = false
frontier_candidate_ids exactly equal the six unique frontier records
```

No manual `--connector-ids` override is allowed.

Every manifest connector ID must exist exactly once in the canonical A2 service graph. Missing or duplicate IDs are fatal.

## 8. Current-Outcome Integrity Gate

For each of the six selected rerun validations, the harness must reconstruct current evidence as:

```text
status
backend_status
search_expansions
queue_exhausted
expansion_budget_reached
failure_class
```

These six fields must exactly equal the manifest's operator-frozen A3.6 evidence for the same connector ID.

Any mismatch is:

```text
A3.8 current outcome drift
```

and is fatal. Detailed counters must not be interpreted after drift.

This gate ensures the selective rerun is observationally consistent with the prior A3.6 experiment before using its newly surfaced diagnostics.

## 9. Diagnostic Output

For each frontier connector the report keeps three separate namespaces:

```text
a37_frontier
    operator-frozen topology, A3.6 outcome evidence and A4 witness

current_evidence
    selective rerun status/backend/termination/failure-class fields

current_reverse_backend_diagnostics
    complete current five-curvature R6B diagnostic mapping
```

Detailed current diagnostics include:

```text
start_goal_position_error_m
best_goal_position_error_m
best_goal_yaw_error_rad
best_goal_distance_state_direction
best_goal_distance_cusp_count
nodes_popped
stale_nodes_skipped
cusp_switches_considered
cusp_switches_rejected_state_dominance
cusp_switches_enqueued
nodes_at_max_cusps
primitive_edges_considered
primitive_edges_rejected_search_envelope
primitive_edges_rejected_site_boundary
primitive_edges_rejected_navigation_grid
primitive_edges_rejected_path_length
primitive_edges_rejected_state_dominance
primitive_edges_enqueued
goal_tolerance_checks
goal_tolerance_successes
goal_shot_attempts
goal_shot_reverse_direction_blocked
goal_shot_candidates_considered
goal_shot_rejected_path_length
goal_shot_rejected_search_envelope
goal_shot_rejected_site_boundary
goal_shot_rejected_navigation_grid
goal_shot_successes
```

## 10. Descriptive Evidence Only

A3.8 may report deterministic distributions across the six rows. It must not synthesize causal or amendment conclusions.

Forbidden semantic keys remain:

```text
route_ready
reachable_from_start
optimal
```

The report also must not synthesize:

```text
root_cause
recommended_amendment
```

Those are decisions for the post-A3.8 evidence review.

## 11. Future Harness Contract

Future path:

```text
tools/v25_12g_a38_selective_frontier_diagnostics.py
```

Frozen constants:

```text
REPORT_SCHEMA = agt_v25_12g_a38_selective_frontier_diagnostics/v1
VALIDATION_SCOPE = A38_SELECTIVE_A4_FRONTIER_CURRENT_DIAGNOSTIC_ONLY_NOT_PLANNER_AMENDMENT_NOT_ROUTE_READY
A37_MANIFEST_SCHEMA = agt_v25_12g_a37_operator_frontier_manifest/v1
EXPECTED_FRONTIER_COUNT = 6
```

Required helpers:

```text
build_parser()
validate_frontier_manifest(manifest)
frontier_connector_ids(manifest)
select_frontier_connector_candidates(all_candidates, manifest)
compare_selective_outcomes(manifest, validations)
build_selective_report(manifest, validations)
assert_no_forbidden_semantic_keys(value, path="root")
```

Parser inputs:

```text
--run-dir           required
--vehicle-profile   required
--a37-manifest      required
--service-graph     default vehicle_feasible_service_graph.yaml
--turn-zones        default turn_zones.yaml
--navigation-map    default navigation_map.yaml
--navigation-pgm    default navigation_map.pgm
--site-boundary     default site_boundary.yaml
--pretty            optional
```

No `--a37-report` and no manual `--connector-ids` are allowed.
No output/write flag is allowed. Report JSON goes to stdout.

## 12. Tests-Only RED

Test path remains:

```text
tests/test_v25_12g_a38_frontier_diagnostic_evidence_join_contract.py
```

The filename is retained only because the superseded evidence-join RED was never executed; its contents now exclusively define the approved operator-manifest selective-current-diagnostics contract.

Commits:

```text
b3984789019b93ca53e1042d7b3fb63ccf4802dc
docs(v25-12g): persist operator A3.7 frontier manifest

685ce73880ca6ac1eaec97d38f1a2cf2dae098ea
test(v25-12g): bind A3.8 RED to operator frontier manifest
```

The future A3.8 harness does not exist yet.

## 13. Exact Target-Machine G1 RED Gate

Synchronize safely:

```bash
cd ~/agt_navigation_v2

git status --short

git fetch origin feat/v25-12g-maximum-feasible-coverage
git merge --ff-only origin/feat/v25-12g-maximum-feasible-coverage

git rev-parse HEAD
git status --short
```

Confirm the persistent manifest is present:

```bash
test -f docs/v2.5/evidence/V25_12G_A37_OPERATOR_FRONTIER_MANIFEST_2026-08-18.json
echo "A37_MANIFEST_EXISTS=$?"
```

Expected:

```text
A37_MANIFEST_EXISTS=0
```

No `/tmp` A3.5/A3.6/A3.7 artifact is required for G1.

Then run only the A3.8 RED contract:

```bash
source /opt/ros/humble/setup.bash
[ -f install/setup.bash ] && source install/setup.bash

PYTHONPATH="$PWD/src/agt_offline_assets:${PYTHONPATH:-}" \
python3 -m pytest -q \
  tests/test_v25_12g_a38_frontier_diagnostic_evidence_join_contract.py \
  -k a38
```

Expected selected test instances:

```text
10
```

Valid RED requires:

```text
Passed: 0
Failed: 10
Errors: 0
```

Every failure must be caused by:

```text
missing A3.8 selective-frontier harness:
tools/v25_12g_a38_selective_frontier_diagnostics.py
```

Invalid RED examples:

```text
collection error
SyntaxError
fixture construction failure
unexpected import error
existing selective harness unexpectedly present
```

## 14. Stop After G1

After the RED command, stop and report. Do not implement the harness locally and do not run the six real connectors yet.

Report:

```text
HEAD
A37_MANIFEST_EXISTS
Collected
Selected
Passed
Failed
Errors
Deselected
Failure family counts
git status --short
git diff --cached --name-only
```

## 15. After VALID RED

Only after operator-supplied VALID RED may production add:

```text
tools/v25_12g_a38_selective_frontier_diagnostics.py
```

Then the target machine runs synthetic GREEN first.

Only after GREEN will A3.8 execute one controlled real-data selective run of exactly six frontier connectors. The resulting JSON must be persisted outside `/tmp` as repository/runtime evidence before the stage is frozen.

A3.8 does not rerun all 40 connectors, does not amend planner behavior and does not advance A4 by itself.

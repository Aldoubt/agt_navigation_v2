# V25-12G A3.8 Frontier Diagnostic Evidence-Join RED Handoff

Date: 2026-08-18

Status: **A3.6 FROZEN / A4 BLOCKED / A3.7 FRONTIER OBSERVED / A3.8 TESTS-ONLY RED LANDED**

Stage:

```text
V25-12G A3.8 — Frontier Diagnostic Evidence Join
```

A3.8 is diagnostic-only. It does not modify planner behavior and does not run any R6B derivation.

## 1. Repository / Branch

```text
Repository: Aldoubt/agt_navigation_v2
Branch: feat/v25-12g-maximum-feasible-coverage
Workspace: ~/agt_navigation_v2
ROS: ROS2 Humble
```

Use the original checkout only. Do not create another worktree.

Protected unrelated local paths remain:

```text
M  tools/rosbag_sensor_trimmer
?? src/agt_route_benchmark/
?? tools/map_tools/render_pcd_top_views.py
```

Never modify, reset, delete, rename, restore or stage them.

Do not use:

```text
git add .
git add -A
git clean
git reset --hard
git restore .
git checkout -- .
```

## 2. Why A3.8 Changed From New Instrumentation To Evidence Join

The A3.7 frontier output correctly found six A4 one-edge frontier connectors, but its `a35_reference` detailed diagnostic fields were null.

This is not a `reverse_primitive_diagnostics.py` schema defect.

The frozen A3.5 runtime `vehicle_feasible_motion_graph.yaml` is a behavior/topology asset and was intentionally not rewritten by the A3.5 diagnostic-only experiment. The complete A3.5 per-connector diagnostics were emitted only by the one-and-only-one A3.5 G3 JSON report.

The original saved target-machine A3.5 report path is:

```text
/tmp/v25_12g_a35_connector_diagnosis.json
```

The current A3.7 frontier report path is:

```text
/tmp/v25_12g_a37_chain_frontier.json
```

Therefore A3.8 must first join these two already-saved reports before any new instrumentation or selective rerun is considered.

## 3. A3.8 Question

A3.8 asks only:

> For each A3.7 A4-frontier connector, what did the frozen A3.5 G3 detailed counters actually report, and does that existing evidence distinguish goal/terminal, state-dominance, or hard-gate pressure strongly enough to choose the next hypothesis without rerunning the planner?

A3.8 does not itself choose or implement a planner amendment.

## 4. Evidence Sources

A3.8 consumes exactly:

```text
A3.5 G3 report:
/tmp/v25_12g_a35_connector_diagnosis.json
schema = agt_v25_12g_a35_connector_backend_diagnosis/v1

A3.7 frontier report:
/tmp/v25_12g_a37_chain_frontier.json
schema = agt_v25_12g_a37_a4_chain_frontier_diagnosis/v1
```

The A3.5 report contains flattened per-connector R6B diagnostics, including:

```text
failure_class
search_expansions
queue_exhausted
expansion_budget_reached
start_goal_position_error_m
best_goal_position_error_m
best_goal_yaw_error_rad
best_goal_distance_state_direction
best_goal_distance_cusp_count
primitive-edge counters
cusp-state counters
goal-tolerance counters
goal-shot counters
```

The A3.7 report contains:

```text
current A3.6 outcome evidence
A4 one-edge frontier topology
hypothetical predecessor/successor witnesses
```

These provenance namespaces must remain separate.

## 5. No Re-Derivation Contract

Future A3.8 harness path:

```text
tools/v25_12g_a38_frontier_diagnostic_evidence_join.py
```

It must not contain or call:

```text
derive_vehicle_feasible_motion_graph
derive_reverse_primitive_connector_plan
```

It must not require:

```text
run-dir
vehicle profile
Navigation Grid
Site Boundary
Turn Zones
R6B config
```

Parser inputs are only:

```text
--a35-report   required
--a37-report   required
--pretty       optional
```

No output/write flag is allowed. JSON is emitted to stdout only.

## 6. Frozen Public Contract

```text
REPORT_SCHEMA = agt_v25_12g_a38_frontier_diagnostic_evidence_join/v1
VALIDATION_SCOPE = A38_FRONTIER_DIAGNOSTIC_EVIDENCE_JOIN_ONLY_NOT_PLANNER_AMENDMENT_NOT_ROUTE_READY
A35_REPORT_SCHEMA = agt_v25_12g_a35_connector_backend_diagnosis/v1
A37_REPORT_SCHEMA = agt_v25_12g_a37_a4_chain_frontier_diagnosis/v1
EXPECTED_A35_CONNECTOR_COUNT = 40
```

Required helpers:

```text
build_parser()
join_frontier_evidence(a35_report, a37_report)
build_evidence_join_report(a35_report, a37_report)
assert_no_forbidden_semantic_keys(value, path="root")
```

## 7. Provenance Contract

Each output frontier row must contain separate namespaces:

```text
a37_frontier
    current A3.6 evidence + A3.7 topology/witness data

a35_g3_evidence
    exact flattened connector record from the frozen A3.5 G3 JSON

a35_descriptive_fractions
    deterministic ratios derived only from A3.5 counters
```

A3.7's historical `a35_reference` placeholder must remain visible only inside `a37_frontier`; it must never be promoted as the authoritative A3.5 G3 record.

## 8. Descriptive-Only Derived Values

A3.8 may derive ratios such as:

```text
state_dominance_rejection_fraction
primitive_enqueued_fraction
path_length_rejection_fraction
site_boundary_rejection_fraction
navigation_grid_rejection_fraction
search_envelope_rejection_fraction
```

These are descriptive ratios, not causal probabilities.

The report must not contain synthesized fields such as:

```text
root_cause
recommended_amendment
route_ready
reachable_from_start
optimal
```

The next hypothesis is chosen only after reviewing the real joined counters.

## 9. Tests-Only RED

Tests-only commit:

```text
077e5f18f700902193f1d4e8ec5ffcd2af8607df
test(v25-12g): define A3.8 frontier evidence-join RED contract
```

Test file:

```text
tests/test_v25_12g_a38_frontier_diagnostic_evidence_join_contract.py
```

The future harness does not exist yet.

## 10. Exact Target-Machine G1 RED Gate

Safely synchronize:

```bash
cd ~/agt_navigation_v2

git status --short

git fetch origin feat/v25-12g-maximum-feasible-coverage
git merge --ff-only origin/feat/v25-12g-maximum-feasible-coverage

git rev-parse HEAD
git status --short
```

Confirm the two frozen reports still exist before running the RED test:

```bash
test -f /tmp/v25_12g_a35_connector_diagnosis.json
echo "A35_JSON_EXISTS=$?"

test -f /tmp/v25_12g_a37_chain_frontier.json
echo "A37_JSON_EXISTS=$?"
```

Expected:

```text
A35_JSON_EXISTS=0
A37_JSON_EXISTS=0
```

If either file is missing, stop. Do not rerun A3.5 or A3.6 automatically.

Then run only the A3.8 RED contract:

```bash
source /opt/ros/humble/setup.bash
[ -f install/setup.bash ] && source install/setup.bash

PYTHONPATH="$PWD/src/agt_offline_assets:${PYTHONPATH:-}" \
python3 -m pytest -q \
  tests/test_v25_12g_a38_frontier_diagnostic_evidence_join_contract.py \
  -k a38
```

Expected selected test instances:

```text
10
```

Valid RED requires:

```text
Passed: 0
Failed: 10
Errors: 0
```

and every failure family must be:

```text
missing A3.8 evidence-join harness:
tools/v25_12g_a38_frontier_diagnostic_evidence_join.py
```

Invalid RED examples:

```text
collection error
SyntaxError
fixture-construction error
unexpected import error
existing harness unexpectedly present
```

## 11. Stop After G1

After the RED command, stop and report. Do not implement the A3.8 harness locally and do not run any planner.

Report:

```text
HEAD
A35_JSON_EXISTS
A37_JSON_EXISTS
Collected
Selected
Passed
Failed
Errors
Deselected
Failure family counts
git status --short
git diff --cached --name-only
```

## 12. After VALID RED

Only after operator-supplied VALID RED may production add:

```text
tools/v25_12g_a38_frontier_diagnostic_evidence_join.py
```

Then target-machine GREEN will run the synthetic contract, followed by one read-only join of the already-saved A3.5 and A3.7 JSON files.

No planner rerun is part of A3.8.
